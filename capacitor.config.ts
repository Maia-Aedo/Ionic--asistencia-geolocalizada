import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic--asistencia-geolocalizada',
  webDir: 'www'
};

export default config;
